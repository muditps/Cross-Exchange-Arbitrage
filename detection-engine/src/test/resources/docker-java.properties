# Testcontainers + Docker Desktop on Windows compatibility fix.
# Docker Desktop 29.x requires Docker API version 1.44.
# Without this, docker-java 3.4.x sends API 1.32 requests which
# Docker Desktop rejects with HTTP 400 (BadRequestException).
# See: session-2.8/EVIDENCE.md for full debugging journey.
api.version=1.44
