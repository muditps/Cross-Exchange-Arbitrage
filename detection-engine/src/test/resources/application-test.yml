# ============================================================
# Detection Engine — Test Configuration
# ============================================================
spring:
  kafka:
    bootstrap-servers: ${spring.embedded.kafka.brokers:localhost:29092}
    consumer:
      group-id: detection-engine-test-group
      auto-offset-reset: earliest

arbitrage:
  detection:
    staleness-threshold-ms: 500
    min-spread-bps: 10
    redis-price-ttl-ms: 10000
